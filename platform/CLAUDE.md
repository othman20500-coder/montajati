# platform/ — قواعد خاصة

- القواعد الملزمة في `../CLAUDE.md` (قسم «قواعد منصة الأندلس»)؛ هذا المجلد يستثنى من قاعدة «بلا خادم» لأن خاصية المراجعة بالعين تحتاج دخولًا وصلاحيات.
- بايثون قياسي فقط حتى يُقرر غير ذلك؛ لا اعتماديات خارجية للاختبارات (`unittest`).
- `db/andalus.sqlite` يُبنى بالترحيل ويُلتزم به؛ أي تغيير فيه يمر بمهارة `andalus-evidence` ثم `export_static.py` ثم الاختبارات.
- لا يُدمج شيء قبل نجاح `python -m unittest discover -s platform/tests` وفحوص `andalus_qa_v1_2.py` كلها.
